// data bindings
const previousOperandButtons = document.querySelectorAll(
  "[data-previous-operand]"
);
const currentOperandButtons = document.querySelectorAll(
  "[data-current-operand]"
);
const allClearButtons = document.querySelectorAll("[data-all-clear]");
const deleteButtons = document.querySelectorAll("[data-delete]");
const operationButtons = document.querySelectorAll("[data-operation]");
const numberButtons = document.querySelectorAll("[data-number]");
const equalsButtons = document.querySelectorAll("[data-equals]");

// functionality functions
